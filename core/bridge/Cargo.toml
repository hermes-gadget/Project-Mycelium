[package]
name = "meshemu_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
libloading = "0.8"
mycelium-display = { path = "../display" }
radio_bus = { path = "../radio_bus" }
